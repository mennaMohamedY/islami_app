<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Islami_app_try2" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_dark</item>
        <item name="colorPrimaryVariant">@color/primaryVariant_dark</item>
        <item name="colorOnPrimary">@color/gold_dark</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="NavigationView_color" parent="Theme.Islami_app_try2">
        <item name="backgroundTint">@color/blue_dark</item>
    </style>

    <style name="generaltextColor" parent="Theme.Islami_app_try2">
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="detailedtextColor" parent="Theme.Islami_app_try2">
        <item name="android:textColor">@color/gold_dark</item>
    </style>
</resources>